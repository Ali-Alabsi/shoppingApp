import 'package:flutter/material.dart';

class Spaces {
 static  SizedBox height(double x) {
    return SizedBox(
      height: x,
    );
  }

 static SizedBox width(double x) {
    return SizedBox(
      width: x,
    );
  }
}
